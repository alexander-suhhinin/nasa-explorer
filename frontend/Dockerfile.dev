FROM node:20-alpine

# Create app directory
WORKDIR /app

# Install dependencies first
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start in dev mode
CMD ["npm", "run", "dev", "--", "--host"]\
